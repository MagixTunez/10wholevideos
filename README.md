Hi make sure you have the .env configured to ur domain!!! Happy Reccing!!

Local run //

node client.ts

docker compose up --build

docker compose down
2. `./media` and `./live` are mounted into the container.
3. `./.env` is mounted read-only into the container.
